template <typename Emetteur, typename Recepteur>
float Brasier (Emetteur emetteur, Recepteur recepteur)
{
	float valeur = 4;
	int cout = valeur;
	if (emetteur.getMana() >= 0)
		
	
	return valeur;
}
template <typename Emetteur, typename Recepteur>
float glacier (Emetteur emetteur, Recepteur recepteur)
{
	float valeur = 4;
	
	return valeur;
}

template <typename Emetteur, typename Recepteur>
float foudre (Emetteur emetteur, Recepteur recepteur)
{
	float valeur = 4;
	
	return valeur;
}

template <typename Emetteur, typename Recepteur>
float H2O (Emetteur emetteur, Recepteur recepteur)
{
	float valeur = 4;
	
	return valeur;
}

